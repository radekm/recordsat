RecordSAT is SAT solver which records its actions in LaTeX.

To compile it, you'll need:

  * OCaml
  * Findlib
  * OCaml Batteries Included

Then run command

  make all



To run program, you'll need:

  * Graphviz

Note: progress report will be generated in working directory.
